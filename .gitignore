__pycache__/
*.pyc
.venv/
venv/
banking.db
banking.db-journal

# Log files — contain sensitive activity data
security.log
transactions.log
*.log

# Dev secrets — never commit these
.env
run_dev.bat

# Deployment helper — Windows-only, not needed on server
prepare_deploy.bat

# Wiki files — managed separately via GitHub Wiki
wiki/
